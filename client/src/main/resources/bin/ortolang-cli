#!/bin/bash
CLASSPATH=
for i in `ls ./lib/*.jar`
do
  CLASSPATH=${CLASSPATH}:${i}
done

java -cp ".:${CLASSPATH}" -Dclient.config=./conf/client-config.properties fr.ortolang.diffusion.client.cmd.OrtolangCli $* 
