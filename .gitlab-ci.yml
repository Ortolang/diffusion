image: docker:latest

services:
- docker:dind

variables:
  GIT_SSL_NO_VERIFY: "true"

stages:
  - build
  - package
  - deploy

maven_build:
  image: maven:3-jdk-11-slim
  stage: build
  tags:
    - ortolang-docker-bind
  script:
    - "mvn -DskipTests clean package"