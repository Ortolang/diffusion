image: docker:latest

services:
- docker:dind

variables:
  GIT_SSL_NO_VERIFY: "true"

stages:
  - build
  - package
  - deploy

maven_build:
  image: roffe/kubectl:v1.9.7
  stage: build
  tags:
    - ortolang-docker-bind
  script:
    - "mvn -DskipTests clean package -B"